parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true
    App\:
        resource: '../src/'
    App\Service\WebsiteScreenshotService:
        arguments:
            $projectDir: '%kernel.project_dir%'
